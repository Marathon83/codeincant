# ScriptForge AI — Backend Environment
# Copy to .env and fill in values. Never commit .env to git.

# ANTHROPIC_API_KEY is NOT required with BYOK — users supply their own key.
# Only set this if you want a server-side fallback key for testing.
# ANTHROPIC_API_KEY=sk-ant-...
